<Window x:Class="FocusOnThis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Focus on This - Control"
        Height="50" Width="50"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="0" Top="0">
    <Grid>
        <Border x:Name="ControlButton" 
                Background="#AA333333" 
                CornerRadius="5"
                Margin="5"
                Cursor="Hand"
                MouseEnter="ControlButton_MouseEnter"
                MouseLeave="ControlButton_MouseLeave"
                MouseLeftButtonDown="ControlButton_Click">
            <TextBlock x:Name="StatusIcon"
                       Text="⚫"
                       Foreground="White"
                       FontSize="20"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       ToolTip="Click to toggle Focus Mode"/>
        </Border>
    </Grid>
</Window>
